"""DocOps eval harness — measure RAG quality and catch regressions.

Usage::

    python -m docops eval --suite demo --out artifacts/eval_demo.json
    python -m docops eval --suite demo --retrieval hybrid --rerank on
"""
